{
  "name": "openai-chat-ts",
  "version": "1.0.0",
  "description": "A Spin template to use the OpenAI Chat API with TypeScript",
  "main": "index.js",
  "scripts": {
    "build": "npx webpack --mode=production && mkdir -p target && spin js2wasm -o target/openai-chat-ts.wasm dist/spin.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["openai", "chatgpt", "api", "config", "ts", "typescript", "http"],
  "author": "teknicus",
  "license": "ISC",
  "devDependencies": {
    "@fermyon/spin-sdk": "^0.4.1",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
